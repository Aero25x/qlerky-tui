# qlerky-tui
 terminal TUI for monitoring multiple email inboxes and TOTP 2FA codes. Connects via IMAP, extracts verification codes from emails automatically, and generates authenticator codes — all in one btop-style interface. Pure Python, no dependencies.
